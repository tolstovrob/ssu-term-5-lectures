= 15 октября

/ Теорема (эффективность $overline(X)$).: Пусть $(X_1, X_2, ..., X_n) = overline(X_n) tilde cal(L)_xi (x, theta)$ и $M xi = a < + oo$. Выборочное среднее $overline(x)$ является эффективной оценкой $M xi$.

*Доказательство.* Доказано, что $M overline(X) = a$ (несмещённое). Покажем, что:

$ D overline(X) = inf D limits(theta_n)^tilde $

Рассмотрим $limits(theta_n)^tilde = limits(sum)_(i = 1)^n alpha_i X_i$. $D (theta_n)^tilde$


...
...
...


Покажем состоятельность. Воспользуемся неравенством Чебышёва:

$ forall epsilon > 0 space P{|limits(F_n)^tilde (x) - F_xi (x)| gt.eq.slant } $